prod:
  image: vizzuality/gfw-mail-api:1.0.2${STAGING}
  ports:
    - "3500"
  env_file:
    - prod.env
  command: start
  restart: always
  dns: ${DNS_SERVER}
  log_driver: "syslog"
  log_opt:
    syslog-address: ${SYSLOG_ADDRESS}
    tag: gfw-mail-api${STAGING}
